import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))

// this file starts the application!
// the .render() method takes the first component (the main one, called App)
// and mounts it in an initially empty div (you can find it in /public/index.html)
